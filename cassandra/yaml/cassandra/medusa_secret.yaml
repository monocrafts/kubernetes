apiVersion: v1
kind: Secret

metadata:
  name: cassandra-medusa-credentials
  namespace: cassandra

type: Opaque

stringData:
  credentials: |
    [default]
    aws_access_key_id = <MINIO_ACCESS_KEY>
    aws_secret_access_key = <MINIO_SECRET_KEY>
